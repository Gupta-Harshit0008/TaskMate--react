import logo from '../assets/logo.svg';

export const HEADERNAV = () => {
  return (
    <header className=''>
      <span className="logo">
        <img src={logo} alt="Logo" />
        <span>Taskmate</span>
      </span>
      
      
      </header>
  )
}
